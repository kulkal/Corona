-- Minimal editor bootstrap. This mode intentionally does not spawn scene
-- geometry; C++ loads assets/maps/editor_empty.map only when no map was
-- specified on the command line.

corona.log("[editor] bootstrap ready")
